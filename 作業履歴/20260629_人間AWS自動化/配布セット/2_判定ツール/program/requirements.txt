# 判定ツールの依存パッケージ
# 導入:  pip install -r requirements.txt
openpyxl
PyYAML
truststore ; python_version >= "3.10"   # 社内TLS検査プロキシ対応（OSの証明書ストアを使う）
anthropic                                 # BACKEND="api"（従量課金）で使う場合のみ必要。サブスク運用では不要
